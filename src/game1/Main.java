/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package game1;

import Sockets.ServerRead;
import Sockets.ServerConnection;
import java.io.DataOutputStream;
import java.net.Socket;

class Main {
 
public static void main(String argv[]) throws Exception
{
interfaceStart st=new interfaceStart();
//ServerOut hd=new ServerOut();
//Thread td=new Thread(hd);
//td.start();
//ServerIn in=new ServerIn();
//Thread in1=new Thread(in);
//in1.start();
//Thread.sleep(15000);
//clientSocket = new Socket("localhost", 6000);
//outToServer =new DataOutputStream(clientSocket.getOutputStream());
////BufferedReader inFromServer =new BufferedReader(new InputStreamReader(clientSocket.getInputStream()));
////sentence = inFromUser.readLine();
//
//System.out.println("yes");
//outToServer.writeBytes("RIGHT#");
//clientSocket.close();
//Thread.sleep(5000);
//clientSocket = new Socket("localhost", 6000);
//outToServer =new DataOutputStream(clientSocket.getOutputStream());
////BufferedReader inFromServer =new BufferedReader(new InputStreamReader(clientSocket.getInputStream()));
////sentence = inFromUser.readLine();
//
//System.out.println("yes");
//outToServer.writeBytes("SHOOT#");
//clientSocket.close();
////KeyEvents key=new KeyEvents();
////Thread td1=new Thread(key);
////td1.start();
}
}